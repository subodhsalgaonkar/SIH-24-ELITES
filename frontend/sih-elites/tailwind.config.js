/** @type {import('tailwindcss').Config} */
export const content = [
  './index.html',
  "./src/**/*.{js,jsx,ts,tsx}", // Adjust this path if your source files are in a different directory
];
export const theme = {
  extend: {},
};
export const plugins = [];
